# 홈페이지 수정본 업로드 방법

1. 이 폴더 안의 파일과 폴더를 모두 선택합니다.
2. GitHub 저장소 `SD608/sd-center`에서 `Code → Add file → Upload files`로 들어갑니다.
3. 기존 파일 위에 그대로 덮어쓰고 `Commit changes`를 누릅니다.
4. 1~5분 뒤 홈페이지에서 `Ctrl + F5`를 누릅니다.

이 수정본은 다운로드 주소를 SD608 저장소로 직접 고정해 `홈페이지 설정 필요` 팝업이 나타나지 않습니다.

plugins {
    id 'com.android.application'
}

android {
    namespace 'com.sd608.sdcenter'
    compileSdk 35

    defaultConfig {
        applicationId 'com.sd608.sdcenter'
        minSdk 24
        targetSdk 35
        versionCode 1
        versionName '1.0.0-mobile-beta'
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

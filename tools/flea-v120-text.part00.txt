--- a/sd-flea-market/main.js
+++ b/sd-flea-market/main.js
@@ -100,6 +100,56 @@
 const RED_DIAMOND_NAME = "레드 다이아몬드";
 const RED_DIAMOND_CHANCE_DENOMINATOR = 100000;
 
+
+function itemDictionaryCatalog() {
+  const tierNames = Object.fromEntries(BOX_TIERS.map((tier) => [tier.id, tier.name]));
+  const rows = [];
+  for (const tierId of ["worn", "normal", "fancy", "premium"]) {
+    const pool = ITEMS[tierId] || [];
+    const chance = pool.length ? 100 / pool.length : 0;
+    for (const [name, price] of pool) rows.push({ name, price, tier: tierId, tierName: tierNames[tierId], dropChance: chance });
+  }
+  rows.push({ name: RED_DIAMOND_NAME, price: null, tier: "safe", tierName: tierNames.safe, dropChance: 100 / RED_DIAMOND_CHANCE_DENOMINATOR, limitedEdition: true });
+  for (const [name, price, weight] of ITEMS.safe) {
+    rows.push({ name, price, tier: "safe", tierName: tierNames.safe, dropChance: (1 - 1 / RED_DIAMOND_CHANCE_DENOMINATOR) * Number(weight || 0) });
+  }
+  return rows;
+}
+
+function itemDictionarySnapshot() {
+  const catalog = itemDictionaryCatalog();
+  const stats = new Map(catalog.map((entry) => [entry.name, { historyCount: 0, currentCount: 0, firstAt: 0 }]));
+  for (const record of Array.isArray(state?.history) ? state.history : []) {
+    const text = String(record?.text || "");
+    if (!text.includes("획득")) continue;
+    const at = Number(record?.at || 0);
+    for (const entry of catalog) {
+      if (!text.includes(`${entry.name} 획득`)) continue;
+      const stat = stats.get(entry.name);
+      stat.historyCount += 1;
+      if (at > 0 && (!stat.firstAt || at < stat.firstAt)) stat.firstAt = at;
+      break;
+    }
+  }
+  for (const item of Array.isArray(state?.items) ? state.items : []) {
+    const stat = stats.get(String(item?.name || ""));
+    if (!stat) continue;
+    stat.currentCount += 1;
+    const at = Date.parse(item?.acquiredAt || "");
+    if (Number.isFinite(at) && at > 0 && (!stat.firstAt || at < stat.firstAt)) stat.firstAt = at;
+  }
+  return catalog.map((entry) => {
+    const stat = stats.get(entry.name);
+    const totalAcquired = Math.max(Number(stat?.historyCount || 0), Number(stat?.currentCount || 0));
+    return {
+      ...entry,
+      acquired: totalAcquired > 0,
+      firstAcquiredAt: stat?.firstAt ? new Date(stat.firstAt).toISOString() : null,
+      totalAcquired,
+    };
+  });
+}
+
 let mainWindow;
 let state;
 let openSessions = new Map();
@@ -116,7 +166,7 @@
 
 function newState() {
   return {
-    schemaVersion: 3,
+    schemaVersion: 4,
     cutterOwned: false,
     stethoscopeOwned: false,
     uvLightOwned: false,
@@ -456,6 +506,7 @@
     prices: { cutter: CUTTER_PRICE, stethoscope: STETHOSCOPE_PRICE, qualityManager: QUALITY_MANAGER_PRICE, backpack: BACKPACK_PRICE, bankFinale: BANK_FINALE_COST },
     boxTiers: BOX_TIERS,
     farmLocations: FARM_LOCATIONS,
+    itemDictionary: itemDictionarySnapshot(),
   };
 }
 
--- a/sd-flea-market/package.json
+++ b/sd-flea-market/package.json
@@ -1,7 +1,7 @@
 {
   "name": "sd-flea-market-pc",
-  "version": "1.1.9",
-  "description": "SD 플리마켓 PC 확장팩 v1.1.1 - v1.0.9 연동 기능 재구축 및 금고 A/D·마우스 조작",
+  "version": "1.2.0",
+  "description": "SD 플리마켓 PC 확장팩",
   "main": "main.js",
   "scripts": {
     "start": "electron .",
--- a/sd-flea-market/sd-app.json
+++ b/sd-flea-market/sd-app.json
@@ -1,10 +1,8 @@
 {
   "id": "sd-flea-market",
   "name": "SD 플리마켓",
-  "version": "1.1.9",
-  "displayVersion": "PC Expansion · v1.1.9",
-  "description": "SD지갑과 SD Link 계정을 연동하는 SD 플리마켓 PC 확장팩입니다. 파밍, 상자 개봉, 은행 습격, 온라인 물품 동기화와 오토바이 추격전을 포함합니다.",
-  "improvement": "은행 습격에서 탈출한 금고를 즉시 개봉하지 않고 상자 보관함에 저장하도록 변경했습니다. 은행 내부 금고 표시는 번호 없이 '금고'로 단순화했습니다.",
+  "version": "1.2.0",
+  "displayVersion": "PC Expansion · v1.2.0",
   "accent": "cyan",
   "entry": "main.js",
   "userDataFolder": "SD플리마켓",
--- a/sd-flea-market/RELEASE_NOTES.txt
+++ b/sd-flea-market/RELEASE_NOTES.txt
@@ -1,30 +1,12 @@
-v1.1.3
-- 은행 피날레에서는 금고를 현장에서 열지 않고 금고 자체를 들고 탈출합니다.
-- 들고 나온 금고 수가 많을수록 오토바이 추격전의 거리, 장애물 빈도, 대형 장애물 확률, 충돌 피해가 증가합니다.
-- 금고 내용물은 추격전 성공 후 은신처에서 개봉되며 금고 1개당 레드 다이아몬드 0.001%(1/100,000) 판정을 1회 수행합니다.
-- 레드 다이아몬드는 기존처럼 판매 불가 한정판 상품입니다.
+SD 플리마켓 v1.2.0
 
-v1.1.2
-- 금고 개봉 1회당 0.001%(1/100,000) 확률로 레드 다이아몬드가 등장합니다.
-- 레드 다이아몬드는 판매할 수 없는 한정판 상품이며 가치 하락 대상에서도 제외됩니다.
-- 은행 금고는 기존처럼 현장에서 직접 개방합니다.
-
-SD 플리마켓 v1.1.1
-- 손상된 v1.0.9 배포본의 릴리스 의도 기능을 정상 v1.0.4 코드 위에 재구축
-- 실제 물류회사 등급: get_sd_flea_company_snapshot 우선 조회 + 기존 진행도 fallback
-- 최신 물류 평판 등급 기준 반영 (S 2,200 / A 1,450 / B 900 / C 520 / D 260 / E 100)
-- 홈 회사 등급 수동 새로고침 추가
-- 물건 보관함 온라인 물품 수동 새로고침 추가
-- 기존 5초 SD Link 자동 재연결·PC↔모바일 재고 동기화·모바일 판매 반영 유지
-- 금고 A/D 키 조작 유지 + 마우스로 다이얼 직접 잡아 회전 추가
-- 종합센터 앱 자동업데이트가 잘못 배포된 v1.1.0에서 교체할 수 있도록 v1.1.1로 배포
-
-SD 플리마켓 PC v1.0.4
-
-- SD 플리마켓을 먼저 실행하고 나중에 SD Link를 열거나 로그인해도 온라인 아이템 동기화를 자동 재시도
-- 백그라운드에서 5초 주기로 PC 인벤토리 ↔ 온라인 보관함 동기화 확인
-- 모바일에서 판매한 아이템도 플리마켓을 계속 켜둔 상태에서 자동으로 PC 보관함에 반영
-- 연결 실패 시 로컬 아이템은 유지하고 다음 주기에 재시도
-- v1.0.3 온라인 마켓/프로필 연동과 v1.0.2 아이템 획득 효과음 유지
-
-주의: SD Link에서 홈페이지 계정 로그인이 유지되어 있어야 하며 Supabase 플리마켓 SQL이 적용되어 있어야 합니다.
+- 36종 아이템 고유 이미지 적용
+- 아이템 사전 추가
+  · 전체 / 상자 등급별 카테고리
+  · 획득 여부
+  · 최초 획득 날짜
+  · 누적 획득 개수
+  · 가격 낮은 순 / 높은 순 정렬
+- 기존 보유 아이템과 과거 획득 기록을 이용한 사전 소급 복원
+- 앱 등록 설명/부가 설명 제거
+- 은행 금고는 상자 보관함에서 직접 개봉하는 v1.1.9 흐름 유지
--- a/sd-flea-market/public/app.js
+++ b/sd-flea-market/public/app.js
@@ -17,6 +17,116 @@
   return `원본 가치 ${Number(item?.originalValue || 0).toLocaleString("ko-KR")}원`;
 }
 
+const ITEM_IMAGE_PATHS = Object.freeze({
+  "볼펜": "assets/items/ballpoint-pen.png",
+  "클립 한 통": "assets/items/paper-clips.png",
+  "지우개": "assets/items/eraser.png",
+  "15cm 자": "assets/items/ruler-15cm.png",
+  "작은 수첩": "assets/items/small-notebook.png",
+  "열쇠고리": "assets/items/keychain.png",
+  "머그컵": "assets/items/mug.png",
+  "USB 케이블": "assets/items/usb-cable.png",
+  "유선 이어폰": "assets/items/wired-earphones.png",
+  "휴대폰 충전기": "assets/items/phone-charger.png",
+  "미니 선풍기": "assets/items/mini-fan.png",
+  "USB 메모리": "assets/items/usb-drive.png",
+  "무선 마우스": "assets/items/wireless-mouse.png",
+  "보조배터리": "assets/items/power-bank.png",
+  "저가형 헤드셋": "assets/items/budget-headset.png",
+  "게임패드": "assets/items/gamepad.png",
+  "기계식 키보드": "assets/items/mechanical-keyboard.png",
+  "브랜드 운동화": "assets/items/brand-sneakers.png",
+  "블루투스 스피커": "assets/items/bluetooth-speaker.png",
+  "무선 이어폰": "assets/items/wireless-earbuds.png",
+  "스마트워치": "assets/items/smartwatch.png",
+  "휴대용 게임기 세트": "assets/items/portable-game-console.png",
+  "프리미엄 헤드폰": "assets/items/premium-headphones.png",
+  "고급 태블릿": "assets/items/premium-tablet.png",
+  "미러리스 카메라": "assets/items/mirrorless-camera.png",
+  "플래그십 스마트폰": "assets/items/flagship-smartphone.png",
+  "고성능 그래픽카드": "assets/items/graphics-card.png",
+  "레드 다이아몬드": "assets/items/red-diamond.png",
+  "금반지": "assets/items/gold-ring.png",
+  "희귀 주화": "assets/items/rare-coin.png",
+  "금목걸이": "assets/items/gold-necklace.png",
+  "금화 세트": "assets/items/gold-coins.png",
+  "명품 시계": "assets/items/luxury-watch.png",
+  "보석 원석": "assets/items/gemstone.png",
+  "소형 금괴": "assets/items/small-gold-bar.png",
+  "대형 금괴": "assets/items/large-gold-bar.png"
+});
+
+
+const ITEM_DICTIONARY_CATALOG = Object.freeze([
+  ...[
+    ["볼펜", 1000], ["클립 한 통", 2000], ["지우개", 1000], ["15cm 자", 2000],
+    ["작은 수첩", 4000], ["열쇠고리", 5000], ["머그컵", 7000], ["USB 케이블", 10000]
+  ].map(([name, price]) => ({ name, price, tier: "worn", tierName: "낡은 상자", dropChance: 12.5 })),
+  ...[
+    ["유선 이어폰", 25000], ["휴대폰 충전기", 30000], ["미니 선풍기", 35000], ["USB 메모리", 40000],
+    ["무선 마우스", 50000], ["보조배터리", 60000], ["저가형 헤드셋", 75000]
+  ].map(([name, price]) => ({ name, price, tier: "normal", tierName: "평범한 상자", dropChance: 100 / 7 })),
+  ...[
+    ["게임패드", 90000], ["기계식 키보드", 120000], ["브랜드 운동화", 150000],
+    ["블루투스 스피커", 180000], ["무선 이어폰", 200000], ["스마트워치", 240000]
+  ].map(([name, price]) => ({ name, price, tier: "fancy", tierName: "고급진 상자", dropChance: 100 / 6 })),
+  ...[
+    ["휴대용 게임기 세트",
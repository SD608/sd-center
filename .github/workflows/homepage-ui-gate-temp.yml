name: Homepage UI Gate TEMP

on:
  push:
    branches:
      - feat/homepage-renewal-v1-20260910
    paths:
      - index.html
      - assets/css/home.css
      - assets/js/home.js
      - .github/workflows/homepage-ui-gate-temp.yml

permissions:
  contents: read
  issues: write
  pull-requests: read

jobs:
  windows-homepage-ui-gate:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Playwright package
        shell: bash
        run: npm install --no-save --ignore-scripts playwright@1.55.0

      - name: Start static server
        shell: bash
        run: |
          python -m http.server 4173 > server.log 2>&1 &
          echo $! > server.pid
          sleep 2
          curl --fail http://127.0.0.1:4173/index.html > /dev/null

      - name: Full homepage responsive and interaction checks
        shell: bash
        run: |
          node <<'NODE'
          const fs = require('fs');
          const { chromium } = require('playwright');
          (async () => {
            const browser = await chromium.launch({ channel: 'chrome', headless: true });
            const widths = [1920, 1536, 1280, 1050, 981, 980, 760, 660, 390, 320];
            const results = [];
            let failed = false;

            for (const width of widths) {
              const page = await browser.newPage({ viewport: { width, height: 1100 } });
              await page.goto('http://127.0.0.1:4173/index.html', { waitUntil: 'domcontentloaded', timeout: 30000 });
              await page.waitForTimeout(1800);

              const base = await page.evaluate(() => {
                const visible = el => !!el && !el.hidden && getComputedStyle(el).display !== 'none' && el.getBoundingClientRect().width > 0 && el.getBoundingClientRect().height > 0;
                const body = document.documentElement;
                const count = document.getElementById('extensionCount');
                const preview = document.getElementById('extensionCountPreview');
                const cards = [...document.querySelectorAll('#extensionGrid .extension-card')];
                const brokenImages = [...document.images].filter(img => img.complete && img.naturalWidth === 0).map(img => img.getAttribute('src'));
                return {
                  overflow: body.scrollWidth > body.clientWidth + 1,
                  extensionCount: count?.textContent?.trim() ?? null,
                  extensionPreview: preview?.textContent?.trim() ?? null,
                  extensionCards: cards.length,
                  brokenImages,
                  loginVisible: [...document.querySelectorAll('[data-auth-guest]')].some(visible),
                  mobileSpotlightVisible: visible(document.querySelector('.sd-mobile-install-spotlight'))
                };
              });

              const expectedCount = Number.parseInt(base.extensionCount || '0', 10);
              const countOk = String(expectedCount) === base.extensionPreview && expectedCount === base.extensionCards;
              let navOk = true;
              let escFocusOk = true;
              let linkCloseOk = true;

              if (width <= 980) {
                const toggle = page.locator('.home-nav-toggle');
                await toggle.click();
                navOk = await toggle.getAttribute('aria-expanded') === 'true' && await page.locator('#homeNav').isVisible();
                await page.keyboard.press('Escape');
                escFocusOk = await toggle.getAttribute('aria-expanded') === 'false' && await toggle.evaluate(el => document.activeElement === el);
                await toggle.click();
                await page.locator('#homeNav > a').first().click();
                linkCloseOk = await toggle.getAttribute('aria-expanded') === 'false';
              }

              const mobileOk = base.mobileSpotlightVisible === (width <= 760);
              const ok = !base.overflow && countOk && navOk && escFocusOk && linkCloseOk && mobileOk && base.loginVisible && base.brokenImages.length === 0;
              if (!ok) failed = true;
              results.push({ width, ok, overflow: base.overflow, countOk, navOk, escFocusOk, linkCloseOk, mobileOk, loginVisible: base.loginVisible, brokenImages: base.brokenImages });
              await page.screenshot({ path: `homepage-${width}.png`, fullPage: true });
              await page.close();
            }

            fs.writeFileSync('ui-gate-result.json', JSON.stringify(results, null, 2));
            console.log('UI_GATE_JSON=' + JSON.stringify(results));
            await browser.close();
            if (failed) process.exit(1);
          })().catch(err => { console.error(err); process.exit(1); });
          NODE

      - name: Install Lighthouse
        shell: bash
        run: npm install --no-save --ignore-scripts lighthouse@12.8.2

      - name: Lighthouse localhost baseline
        shell: bash
        run: |
          npx lighthouse http://127.0.0.1:4173/index.html --quiet --chrome-flags='--headless --no-sandbox' --output=json --output-path=lighthouse.json

      - name: Record metrics on PR
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          node <<'NODE'
          const fs = require('fs');
          const lh = require('./lighthouse.json');
          const ui = require('./ui-gate-result.json');
          const pct = v => Math.round((v || 0) * 100);
          const widths = ui.map(x => `${x.width}:${x.ok ? 'PASS' : 'FAIL'}`).join(', ');
          const out = {
            performance: pct(lh.categories.performance.score),
            accessibility: pct(lh.categories.accessibility.score),
            bestPractices: pct(lh.categories['best-practices'].score),
            seo: pct(lh.categories.seo.score),
            lcpMs: Math.round(lh.audits['largest-contentful-paint'].numericValue),
            cls: Number(lh.audits['cumulative-layout-shift'].numericValue.toFixed(4)),
            tbtMs: Math.round(lh.audits['total-blocking-time'].numericValue)
          };
          const body = [
            '<!-- homepage-ui-gate-temp-v2 -->',
            '### Homepage UI Gate TEMP — Windows evidence',
            '',
            '- head SHA: ' + process.env.GITHUB_SHA,
            `- Windows Chrome responsive/interactions: **${ui.every(x => x.ok) ? 'PASS' : 'FAIL'}**`,
            `- widths: ${widths}`,
            `- Lighthouse localhost: Performance **${out.performance}**, Accessibility **${out.accessibility}**, Best Practices **${out.bestPractices}**, SEO **${out.seo}**`,
            `- LCP: **${out.lcpMs} ms**, CLS: **${out.cls}**, TBT: **${out.tbtMs} ms**`,
            '',
            '> 범위: GitHub Actions `windows-latest` + headless Chrome + localhost 전체 저장소 렌더. 실제 사용자 Windows 시각 확인, 운영 Supabase 실계정, Android/iPhone 실기기 검증을 대체하지 않음.'
          ].join('\n');
          fs.writeFileSync('pr-comment.md', body);
          fs.writeFileSync('metrics.json', JSON.stringify(out, null, 2));
          NODE
          gh pr comment 83 --body-file pr-comment.md

      - name: Upload evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: homepage-ui-gate-temp-v2
          path: |
            homepage-*.png
            lighthouse.json
            ui-gate-result.json
            metrics.json
            pr-comment.md
            server.log
          if-no-files-found: ignore
